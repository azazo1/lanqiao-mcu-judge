# 串口

接收串口 Uart1 信息并且输出.

接收格式为 uint, 如果成功解析出 uint, 那么返回其值 + 1,
并且其原值被显示在 seg 右五位中, 0 填充.
seg 前三位显示的是 uart1_isr 被 TI 触发的次数, 并且有一个小数点.

如果输入不符合格式, 串口输出 Error.

和 samples/uart 不同的是, putchar 在 `TI = 0` 之前故意延迟了几个空指令, 以观察 TI 中断的重复触发.
