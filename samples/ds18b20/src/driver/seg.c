#include "seg.h"

u8 code sg_dula[] = {
	0x3F,0x06,0x5B,0x4F,0x66,0x6D,0x7D,0x07,0x7F,0x6F,0x00,
	// A, H, P
	0x77, 0x76, 0x73
};

void sg_disp(u8 pos, u8 dula, bit dot) {
	write0(0xe0, 0xff);
	write0(0xc0, 1 << pos);
	write0(0xe0, ~(sg_dula[dula] | (dot ? 0x80 : 0x00)));
}